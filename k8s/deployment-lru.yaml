apiVersion: apps/v1
kind: Deployment
metadata:
  name: lru
  namespace: app-test
spec:
  selector:
    matchLabels:
      app: lru
  template:
    metadata:
      labels:
        app: lru
    spec:
      containers:
      - name: lru
        image: redis:7.0.4
        command: [ "redis-server", "/conf/redis.conf"]
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 6379
        volumeMounts:
          - name: conf
            mountPath: /conf/
          - name: data
            mountPath: /data/

      volumes:
        - name: data
          emptyDir: {}
        - name:  conf
          configMap:
            name: redis-config
         

            


